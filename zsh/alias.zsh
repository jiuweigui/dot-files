# Alias for SSH so that it "supports" includes
alias generate-ssh-config='cat ~/.ssh/config.d/* > ~/.ssh/config'

# Aliases for MARKS
alias j='jump'
alias ml='marks'

# Alias for tmuxifier
alias mux='tmuxifier'
